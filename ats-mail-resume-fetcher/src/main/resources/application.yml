spring:
  application:
    name: 'ats-mail-resume-fetcher'

server:
  port: 0

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.uuid}

---
spring:
  config:
    activate:
      on-profile: docker

server:
  port: 8080
